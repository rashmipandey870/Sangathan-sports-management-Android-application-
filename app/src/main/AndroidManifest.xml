<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- INTERNET permission -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SangathanApp"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".ui.trial.StudentTrialActivity"
            android:exported="false" />
        <activity
            android:name=".ui.events.StudentTeamActivity"
            android:exported="false" />
        <activity
            android:name=".ui.headadmin.HeadAdminDashboardActivity"
            android:exported="false" />
        <activity
            android:name=".ui.captain.CaptainDashboardActivity"
            android:exported="false" />
        <activity
            android:name=".ui.RoleSelectionActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        <activity
            android:name=".DashboardActivity"
            android:exported="false" /> <!-- ================= SPORT REGISTRATION ================= -->
        <activity
            android:name=".ui.registration.SportRegistrationActivity"
            android:exported="false" /> <!-- ================= TRIAL SCHEDULE ================= -->
         <!-- ================= EMAIL VERIFICATION ================= -->
        <activity
            android:name=".ui.verification.EmailVerificationActivity"
            android:exported="false" /> <!-- ================= SETTINGS ================= -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false" /> <!-- ================= COORDINATOR DASHBOARD ================= -->
        <activity
            android:name=".ui.coordinator.CoordinatorDashboardActivity"
            android:exported="false" />
        <activity
            android:name=".ui.coordinator.TeamSelectionActivity"
            android:exported="false" />
    </application>

</manifest>